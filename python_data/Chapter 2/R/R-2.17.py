#-*-coding: utf-8 -*-

"""
Draw a class inheritance diagram for the following set of classes:

• Class Goat extends object and adds an instance variable_tail and
methods milk( ) and jump( ).
• Class Pig extends object and adds an instance variable_nose and
methods eat(food) and wallow( ).
• Class Horse extends object and adds instance variables_height and
_color, and methods run( ) and jump( ).
• Class Racer extends Horse and adds a method race( ).
• Class Equestrian extends Horse, adding an instance variable_weight
and methods trot( ) and is_trained( ).
"""